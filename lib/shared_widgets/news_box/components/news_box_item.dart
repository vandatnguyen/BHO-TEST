import 'package:flutter/material.dart';

class BoxNewsItem extends StatelessWidget {
  const BoxNewsItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
